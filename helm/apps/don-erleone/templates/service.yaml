apiVersion: v1
kind: Service
metadata:
  name: {{ include "don-erleone.fullname" . }}
  labels:
    {{- include "don-erleone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "don-erleone.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "don-erleone.fullname" . }}-headless
  labels:
    {{- include "don-erleone.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: 4369
      targetPort: epmd
      protocol: TCP
      name: epmd
  selector:
    {{- include "don-erleone.selectorLabels" . | nindent 4 }}