apiVersion: v2
name: cert-manager
description: Cert Manager for automatic TLS (wraps upstream chart)
type: application
version: 0.1.0
dependencies:
  - name: cert-manager
    version: "v1.14.5"
    repository: "https://charts.jetstack.io"
